session: esporte_parana

root: ~/Documents/work/parana-esporte/parana

before_start:
  - echo "echo before start"

stop:
  - echo "echo stop"

windows:
  - name: code
    layout: main-horizontal
    commands:
      - vim
  - name: emulator
    layout: main-horizontal
    commands:
      - emulator_start
  - name: flutter
    layout: main-horizontal
    commands:
      - sleep 10
      - fvm flutter run
