session: nupem

root: /home/shoka/Documents/repos/nupem-org/app-nupem/nupem

windows:
  - name: code
    layout: main-horizontal
    commands:
      - vim
  - name: emulator
    layout: main-horizontal
    commands:
      - emulator_start
  - name: flutter
    layout: main-horizontal
    commands:
      - sleep 10
      - flutter run
