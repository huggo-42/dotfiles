session: frontend

root: ~/Documents/work/...

windows:
  - name: vim
    layout: main-horizontal
    commands:
      - vim
  - name: emulator
    layout: main-horizontal
    commands:
      - emulator_start
  - name: flutter
    layout: main-horizontal
    commands:
      - sleep 15
      - fvm flutter run
